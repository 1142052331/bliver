import { Button, Surface } from '@bliver/ui';
import { useSearchParams } from 'react-router-dom';
import './footprints.css';
export function FootprintDetailRoute({ footprint, onClose }: { readonly footprint: { readonly id: string; readonly message: string; readonly visibility: 'public' | 'friends' | 'private'; readonly locationPrecision: 'precise' | 'approximate' }; readonly onClose?: () => void }) { const [params] = useSearchParams(); const precision = params.get('precision') === 'precise' ? 'precise' : footprint.locationPrecision; return <section className="footprint-detail"><Surface><Button variant="ghost" aria-label="Close footprint" onClick={onClose}>Close</Button><h1>Footprint</h1><p>{footprint.message}</p><p>{footprint.visibility === 'public' ? 'Public' : footprint.visibility === 'friends' ? 'Friends only' : 'Only you'}</p><p>{precision === 'precise' ? 'Precise location' : 'Approximate location'}</p></Surface></section>; }
